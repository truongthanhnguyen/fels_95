<%= link_to gravatar_for(current_user, size: Settings.gravatarsize), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("activity.view_my_profile"), current_user %></span>
<span><%= pluralize current_user.activities.count, t("activity.activity") %></span>
